name: LALInference Pipeline CI
on:
  push:
    branches:
      - master
      - "v*-preview"
      - "v*-release"
  workflow_dispatch:
    inputs:
      gwdata-version:
        description: "asimov-gwdata pip spec or git URL"
        default: "git+https://git.ligo.org/asimov/pipelines/gwdata.git@fix-gwosc-cache"
        required: false
      bilby-version:
        description: "bilby pip spec (used by pesummary)"
        default: "bilby"
        required: false
      bilby-pipe-version:
        description: "bilby_pipe pip spec (used by pesummary)"
        default: "bilby_pipe==1.4.0"
        required: false

jobs:
  lalinference:
    name: "LALInference end-to-end test"
    runs-on: ubuntu-latest
    container:
      image: htcondor/mini:latest
      options: --privileged
    defaults:
      run:
        shell: bash -el {0}
    steps:

      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: ./.github/actions/setup-htcondor

      - name: Install base tools
        run: yum install -y sudo git

      - name: Create submit user
        uses: ./.github/actions/create-submit-user

      - name: Ensure submituser can access workspace
        run: |
          mkdir -p "$GITHUB_WORKSPACE/test_project"
          chmod -R a+rwX "$GITHUB_WORKSPACE"

      - name: Setup Asimov Environment
        uses: ./.github/actions/setup-asimov-env
        with:
          python-version: "3.10"
          extra-packages: >-
            -U
            ${{ inputs.bilby-version || 'bilby' }}
            ${{ inputs.bilby-pipe-version || 'bilby_pipe==1.4.0' }}
            ${{ inputs.gwdata-version || 'git+https://git.ligo.org/asimov/pipelines/gwdata.git@fix-gwosc-cache' }}
            pesummary

      - name: Set up git
        run: |
          su - submituser <<SUBMITUSER
          set -euo pipefail
          git config --global init.defaultBranch master
          git config --global user.email "you@example.com"
          git config --global user.name "Your Name"
          SUBMITUSER

      - name: Start MockGWDataFindServer
        run: |
          python3 << EOF &
          from tests.mock_gwdatafind_server import MockGWDataFindServer
          import time

          frame_configs = {
              ('H', 'H1_LOSC_16_V1'): ['file://$GITHUB_WORKSPACE/tests/test_data/frames/H-H1_GWOSC_16KHZ_R1-1126259447-32.gwf'],
          }

          server = MockGWDataFindServer(port=8765, frame_configs=frame_configs)
          server.start()
          print("MockGWDataFindServer started on port 8765", flush=True)

          try:
              while True:
                  time.sleep(1)
          except KeyboardInterrupt:
              server.stop()
          EOF

          for i in $(seq 1 15); do
            curl -sf http://localhost:8765/api/v1/gwf/H/H1_LOSC_16_V1/1126259460,1126259464/file.json && break || sleep 2
          done

      - name: Create asimov project
        uses: ./.github/actions/run-asimov-command
        with:
          script: asimov init "Test Project"

      - name: Configure asimov
        uses: ./.github/actions/run-asimov-command
        with:
          script: |
            asimov configuration update condor/user submituser
            asimov configuration update condor/cache_time 30
            asimov configuration update condor/cron_minute '*/1'

      - name: Apply project blueprints
        uses: ./.github/actions/run-asimov-command
        with:
          script: |
            asimov apply -f "$GITHUB_WORKSPACE/tests/test_blueprints/gwosc_quick_test.yaml"
            asimov apply -f "$GITHUB_WORKSPACE/tests/test_blueprints/gwosc_event.yaml"

      - name: Submit gwdata job
        env:
          GWDATAFIND_SERVER: http://localhost:8765
        uses: ./.github/actions/run-asimov-command
        with:
          script: |
            asimov apply -f "$GITHUB_WORKSPACE/tests/test_blueprints/gwosc_get_data.yaml" -e GW150914_095045
            asimov manage build submit

      - name: Wait for frame files
        uses: ./.github/actions/wait-for-files
        with:
          patterns: "H*.gwf"
          directory: "working/GW150914_095045/get-data/frames"
          timeout: "600"
          interval: "30"
          working-directory: .

      - name: Wait for gwdata cache file
        uses: ./.github/actions/wait-for-files
        with:
          patterns: "H1.cache"
          directory: "working/GW150914_095045/get-data/cache"
          timeout: "120"
          interval: "10"
          working-directory: .

      - name: Monitor gwdata completion
        uses: ./.github/actions/run-asimov-command
        with:
          script: asimov monitor

      - name: Submit BayesWave PSD job
        env:
          GWDATAFIND_SERVER: https://datafind.gwosc.org
        uses: ./.github/actions/run-asimov-command
        with:
          script: |
            asimov apply -f "$GITHUB_WORKSPACE/tests/test_blueprints/bayeswave_quick_test.yaml" -e GW150914_095045
            asimov manage build submit

      - name: Wait for PSD files
        uses: ./.github/actions/wait-for-files
        with:
          patterns: "trigtime_*/post/clean/glitch_median_PSD_forLI_H1.dat"
          directory: "working/GW150914_095045/generate-psd"
          timeout: "600"
          interval: "30"
          working-directory: .

      - name: Stop BayesWave megaplot job and clear cache
        run: |
          condor_rm -all || true
          rm -f .asimov/_cache_jobs.yaml

      - name: Monitor BayesWave completion
        uses: ./.github/actions/run-asimov-command
        with:
          script: asimov monitor

      - name: Apply and submit LALInference job
        continue-on-error: true
        env:
          GWDATAFIND_SERVER: http://localhost:8765
        uses: ./.github/actions/run-asimov-command
        with:
          script: |
            asimov apply -f "$GITHUB_WORKSPACE/tests/test_blueprints/lalinference_quick_test.yaml" -e GW150914_095045
            asimov manage build submit

      - name: Wait for LALInference result files
        continue-on-error: true
        uses: ./.github/actions/wait-for-files
        with:
          patterns: "posterior_samples/posterior*.hdf5"
          directory: "working/GW150914_095045/lalinference-IMRPhenomXPHM"
          timeout: "2400"
          interval: "30"
          working-directory: .

      - name: Monitor LALInference completion
        continue-on-error: true
        uses: ./.github/actions/run-asimov-command
        with:
          script: asimov monitor

      - name: Show LALInference analysis status
        run: |
          python3 -c "
          import yaml
          with open('.asimov/ledger.yml') as f:
              ledger = yaml.safe_load(f)
          for ev in ledger.get('events', []):
              for prod_dict in ev.get('productions', []):
                  for pname, prod in prod_dict.items():
                      if 'lalinference' in pname.lower():
                          print(f'{pname}: {prod.get(\"status\", \"unknown\")}')
          " || true

      - name: Archive testing artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lalinference-ci-artifacts
          path: |
            checkouts/
            working/
          retention-days: 5

      - name: Stop background server
        if: always()
        run: pkill -f "MockGWDataFindServer" || true
